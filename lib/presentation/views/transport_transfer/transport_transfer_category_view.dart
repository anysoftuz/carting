
import 'package:flutter/material.dart';

class TransportTransferCategoryView extends StatelessWidget {
  const TransportTransferCategoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}